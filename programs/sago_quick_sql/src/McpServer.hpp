#ifndef MCPSERVER_HPP
#define MCPSERVER_HPP

// Runs the MCP (Model Context Protocol) server using newline-delimited
// JSON-RPC 2.0 over stdin/stdout. Returns process exit code.
int RunMcpServer();

#endif /* MCPSERVER_HPP */
